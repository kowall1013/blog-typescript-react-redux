//Inner
import { ArticlesList } from './components';

//Styles
import { Wrapper } from './Start.css';

export const Start: React.FC = () => {
  return (
    <Wrapper>
      <ArticlesList />
    </Wrapper>
  );
};
